#!/bin/sh

hash opencode 2>/dev/null || { echo "ERROR: 'opencode' is not installed" && exit 1; }

WAIT_TIME="1 hour"

echo "Suggesting an activity for '${WAIT_TIME}' waiting time..."

opencode run <<'EOF' | glow -p
I need ${WAIT_TIME} break because my AI is rate-limited. Suggest a fun activity while I wait.
- Maybe there's Github PR or issues that I can tackle? You can use `gh` cli to check
- Maybe there's TODO items in the codebase that is easy to solve?
- Or maybe there's something new I can learn...
EOF

